# Web Weather App Backend

Select (or install) proper node: `nvm use`

Install all dependencies: `npm i`

Configure your [Dark Sky](https://darksky.net/dev) API key in: `.darkskykeyrc`

Start app: `npm start`
